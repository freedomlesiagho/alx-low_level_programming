#include <stdio.h>

/**
 * main - This is where the main coding begins
 *
 * Return: Will always be 0 if successful
 */

int main(void)
{
	int digit;

	for (digit = 0; digit < 10; digit++)
		printf("%d", digit);

	printf("\n");

	return (0);
}
